---
applications:
- name: log-demo
  memory: 512M
  instances: 1
  path: target/spring-envoronment-variables.war
  env:
     host: host
     port: 5672
     virtualhost: virtualhost
     username: user
     password: password
     log_service: rabbitmq-spring
  services:
     - rabbitmq-spring
